import { StyledFooter, P } from "../../../../styles/componentLayout"

export const Footer = (): any => {

  return (
    <StyledFooter>
      <P>Todas as marcas e ilustrações utilizadas são de seus resepctivos donos.</P>
    </StyledFooter>
  )
}